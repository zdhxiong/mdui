import { dedupeMixin } from '@open-wc/dedupe-mixin';

const RippleMixin = dedupeMixin(
  superclass =>
    class extends superclass {
      // your mixin code goes here
    },
);

export default RippleMixin;
